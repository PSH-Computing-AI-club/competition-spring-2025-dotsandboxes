// Since the Deno CLI does not support globs yet, we are using this script to
// traverse the codebase.

import '../cli/mod.ts';
import '../engine/mod.ts';
import '../util/mod.ts';
import '../worker/mod.ts';
